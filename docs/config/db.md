# Database configuration

## `path`

Path to database file. (default `ftpgrab.db`)

!!! example "Config file"
    ```yaml
    db:
      path: ftpgrab.db
    ```

!!! abstract "Environment variables"
    * `FTPGRAB_DB_PATH`
